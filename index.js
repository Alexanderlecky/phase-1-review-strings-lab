// Write your code in this file!
// Define the currentUser variable
const currentUser = "Grace Hopper";

// Define the welcomeMessage using template literals
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define the excitedWelcomeMessage, transforming welcomeMessage to uppercase
const excitedWelcomeMessage = welcomeMessage.toUpperCase();

// Define the shortGreeting, using the first character of currentUser
const shortGreeting = `Welcome, ${currentUser.charAt(0)}!`;






